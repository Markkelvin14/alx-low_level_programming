#include "main.h"

/**
 * mul - a function that multiplies two integers
 * @a: is to be multipled by b
 * @b: is to be multiplied by a
 * Return: mul
 */

int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
