#include "main.h"
/**
 * add - sdds two integers and returns the result
 * @m: interger to be added
 * @n: interger to be added
 * Return: sum
 */
int add(int m, int n)
{
	int i;

	i = m + n;
	return (i);
}
