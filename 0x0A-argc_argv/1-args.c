#include "main.h"
#include <stdio.h>

/**
 * main - prints a num of arguements
 * @argc: num of arg
 * @argv: arrays
 * Return: num
 */

int main(int argc, char *argv[]__attribute__((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}
