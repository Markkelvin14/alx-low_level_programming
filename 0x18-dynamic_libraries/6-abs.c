#include "main.h"
/**
 * _abs - a function that computes the a absolute value of an integer
 * @i: the integer to be checked
 * Return: absolute value of the integer
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
