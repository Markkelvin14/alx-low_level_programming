#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry
 * Return: Always 0
 */
int main(void)
{
	int i;

	i = 0;
	while (i <= 10)
	{
		printf("%d\n", i);
		i++;
	}
	return (0);
}
